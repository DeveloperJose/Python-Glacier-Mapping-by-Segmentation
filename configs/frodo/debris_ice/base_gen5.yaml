# Gen5 Debris Ice Baseline - Frodo
# Uses base dataset (no physics/velocity) - FIXED binary loss
training_opts:
  dataset_name: bibek_w512_o64_f1_v2
  run_name: dci_base_gen5

loader_opts:
  physics_channels: false
  velocity_channels: false