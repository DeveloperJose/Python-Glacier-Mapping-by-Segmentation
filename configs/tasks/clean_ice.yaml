# Clean Ice (CI) binary segmentation task defaults
loader_opts:
  output_classes: [1]  # 0=BG, 1=CleanIce (ignore debris)
loss_opts:
  foreground_classes: [1]  # Explicit foreground class for binary loss
metrics_opts:
  threshold: [0.5, 0.5]  # BG, CleanIce
