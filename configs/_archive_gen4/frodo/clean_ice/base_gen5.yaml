# Gen5 Clean Ice Baseline - Frodo
# Uses base dataset - FIXED binary loss
training_opts:
  dataset_name: bibek_w512_o64_f1_v2
  run_name: ci_base_gen5

loader_opts:
  physics_channels: false
  velocity_channels: false