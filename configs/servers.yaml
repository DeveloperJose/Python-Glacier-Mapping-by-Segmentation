# Portable defaults. Override any value in the untracked
# configs/servers.local.yaml without editing this file.
local:
  hostname: localhost
  code_path: .
  output_path: output
  raw_data_path: data/raw
  image_dir: data/raw/Landsat7_2005
  dem_dir: data/raw/DEM
  velocity_dir: data/raw/Velocity
  labels_dir: data/raw/labels
  processed_data_path: data/processed/HKH
  num_workers: 2
