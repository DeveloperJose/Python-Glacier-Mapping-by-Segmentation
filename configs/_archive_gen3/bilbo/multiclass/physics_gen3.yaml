# Gen3 Multi-class with physics - Bilbo (150 epochs)
# Tests physics channels on multi-class (best CI config: phys64_s075)
training_opts:
  dataset_name: bibek_w512_o64_f1_v2_phys64_s075
  run_name: multi_physics_gen3

loader_opts:
  output_classes: [0, 1, 2]  # All three classes: BG, CleanIce, Debris
  physics_channels: true        # [20-23] flow_accumulation, tpi, roughness, curvature

loss_opts:
  foreground_classes: [1, 2]  # Critical fix: include both glacier classes for loss