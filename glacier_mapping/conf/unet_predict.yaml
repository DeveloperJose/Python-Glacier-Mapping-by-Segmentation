runs_dir: "/home/devj/local-debian/glacier_mapping/debian-runs/"
output_dir: "/home/devj/local-debian/glacier_mapping/debian-pred-runs/"
# gpu_rank: "cpu"
gpu_rank: 0

visualization:
  mode: "scaled" # "scaled" (Option C) or "fullres" (Option B)
  max_width: 2400 # Only used when mode="scaled"

# model_type can be a specific checkpoint, "best", "final", or "all"
cleanice:
  run_name: "replicate_bibek_cleanice_all_channels_v2"
  threshold: 0.5
  model_type: "epoch0143_val0.224681"

debris:
  # run_name: "replicate_bibek_debrisice_all_channels_v2_phys64_v3"
  # run_name: "replicate_bibek_debrisice_all_channels_v2_phys64"
  run_name: "replicate_bibek_debrisice_all_channels_v2"
  threshold: 0.5
  # model_type: "all"
  model_type: "epoch0089_val0.456658"
